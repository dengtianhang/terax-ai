# Requirements

- [ ] `REQ-001` UI language switch: Chinese / English, system default, persistent selection.
- [ ] `REQ-002` Terminal color tuning: readable ANSI palette in light theme.
- [x] `REQ-003` Auto-enable ANSI output in PowerShell: Terax-only, no user `$PROFILE` changes.

- [ ] `REQ-005` Image preview: Tauri asset scope matches workspace image files; Markdown local images load through Blob URLs.

